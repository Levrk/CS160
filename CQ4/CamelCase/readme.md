This function runs in linear time complexity because it contains one for loop which iterates once for each letter in the input string

This function runs in constant space complexity because it only initializes two variables regardless of the input size.

